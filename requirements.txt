# ============================================================
# requirements.txt
# ============================================================
# These are the only libraries needed for VERSION 1 of the
# "PDF Chatbot From Scratch" project.
#
# Notice that there is NO openai, NO anthropic, NO cohere, etc.
# Everything here is either:
#   - a general-purpose Python library (PyPDF2, numpy, torch)
#   - a UI library (streamlit)
#
# Install with:
#   pip install -r requirements.txt
# ============================================================

streamlit==1.38.0      # Web UI framework
PyPDF2==3.0.1           # Extract text from PDF files
torch==2.3.1            # Used for vector math (TF-IDF, cosine similarity)
numpy==1.26.4           # Numerical helper operations
